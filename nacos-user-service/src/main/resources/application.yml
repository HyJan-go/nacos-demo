server:
  port: 8208
spring:
  application:
    name: nacos-user-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848  # nacos地址，这里没有用到它的配置中心模块,所以不进行配置
management:
  endpoints:
    web:
      exposure:
        include: '*'